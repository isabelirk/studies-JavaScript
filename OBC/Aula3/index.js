let age = window.prompt("Digite uma idade:");

if (age >= 18) {
  alert("Maior de idade.");
} else if (age <= 12) {
  alert("Criança.");
} else {
  alert("Menor de idade.");
}

const resultado = 6 === 6 ? "Verdadeiro" : "Falso";
console.log(resultado);
